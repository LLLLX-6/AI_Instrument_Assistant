{
  "name": "ai-instrument-assistant-workspace",
  "version": "0.2.0-dev.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "extensions/jlceda"
  ],
  "scripts": {
    "test": "npm run test --workspace @aia/jlceda-extension",
    "build": "npm run build --workspace @aia/jlceda-extension"
  },
  "engines": {
    "node": ">=24"
  }
}
